# AGENTS.md

## Надстройка Excel `Аудиторам`
- Если задача касается корпоративной Excel-надстройки, вкладки `Аудиторам`, папок `addin/` или `addin_updater/`, сборки `AuditAddin.xlam` или обновления самой надстройки, обязательно сначала использовать отдельную инструкцию: `_publish/audit-macros-private/docs/addin-agent-instructions.md`.
- Если задача прямо касается обычного updater-а макросов рабочих книг или его переключения между `Dev` и `Company/Work`, использовать `_publish/audit-macros-private/docs/macro-updater-agent-instructions.md`. Эти legacy-правила не применять к самостоятельным макросам для Google Docs.
- Если задача касается конструктора РД внутри надстройки, смешанного комплекта `Excel + Word`, шаблонов РД или генерации рабочих документов из файла `Анализ существенных статей`, после `addin-agent-instructions.md` использовать `_publish/audit-macros-private/docs/rd-constructor-agent-instructions.md`.
- Для задач по шаблонам РД на этой машине считать обязательной картой путей раздел `Зафиксированная карта путей` в `C:\Users\pavel\OneDrive\Рабочий стол\Шаблоны для конструктора\README.md`. Не искать похожие папки и не брать файлы из `Downloads` или recovery-архивов, если нужный путь уже зафиксирован там. Канонический dev-source каждого РД: `C:\Users\pavel\OneDrive\Рабочий стол\Шаблоны для конструктора\01_projects\<имя проекта>`; книга — в `template`, VBA — в `vba`, требования — в `contract`, входы — в `fixtures`, тесты и прогоны — в `tests`/`80_runs`. `01_workbooks` теперь только read-only generated publish input проектов со статусом `Ready`; вручную его не править и обратно из него не импортировать. Общие инструменты: `...\04_tools`; recovery: `...\99_archive`; старая плоская структура сохранена в `...\99_archive\workspace-v1-20260806-workspace-migration`. Пользовательские три артефакта: `C:\Users\pavel\OneDrive\Рабочий стол\Артефакты шаблонов РД\<имя шаблона>`; локальный company-release шаблонов РД `_publish\audit-macros-private\addin\release\rd-templates-company`; рабочий company-root `Z:\Project\DAVK\Оборудование\Addins`. `Z:` — только целевой путь на рабочих компьютерах; на этой машине его не искать, не монтировать и не менять.
- Для Excel/VBA E2E по шаблонам РД использовать локальный Excel на этой машине. Перед прогоном можно закрывать все `EXCEL.EXE`, затем запускать проверку через локальный Excel COM, локальные PowerShell-скрипты или подготовленную тестовую книгу. Тесты выполнять последовательно на этом компьютере; параллельные Excel-прогоны не запускать. На этой машине должна использоваться обычная рабочая структура `%APPDATA%\Microsoft\AddIns\AuditAddinLoader.xlam`, `%APPDATA%\AuditAddin\installed\AuditAddinLocal.xlam`, `%APPDATA%\AuditAddin\addin-updater.ini`, `%APPDATA%\AuditAddin\assets\Выборка`; для dev-прогонов в `AssetsRoot\Выборка` лежит общая dev-имитация модели и отбора. Тестовые макросы должны отключать лишние модальные окна (`DisplayAlerts=False`, явное закрытие книг, `RemovePersonalInformation=False` перед сохранением при необходимости, `CheckCompatibility=False`) и писать понятный итог/лог, а не ждать скрытый MsgBox или VBA error.
- Для dev-прогонов шаблонов РД выборку выполнять только через универсальные silent-сценарии надстройки `Аудиторам`: `AuditSampling_RunFullSilent`, `AuditSampling_RunModelFromSheetSilent`, `AuditSampling_RunSelectionFromSheetSilent`. Шаблоны не должны дергать UI-кнопки или несайлент-макросы модели/отбора, потому что мастер-сборка РД должна идти без лишних всплывающих окон. В dev эти silent-сценарии используют общую локальную имитацию модели и отбора из `AssetsRoot\Выборка`; в company-release те же silent-сценарии используют реальные корпоративные файлы модели и отбора из рабочего `AssetsRoot\Выборка`. Не создавать отдельные файлы модели/отбора и папки `_dev_sampling_<код шаблона>` под конкретный РД. По конкретному РД меняются только входные данные, числа, риски, правила подготовки популяции и количество групп `Модель_N`/`Отбор_N`, но не сам движок модели/отбора.
- Для задач по шаблонам РД последний проверенный company-release надстройки считать внешним источником правды и стабильным контрактом. Нельзя править `addin/`, `addin_updater/`, `AuditAddin.xlam`, loader/setup или логику `AuditSampling_*` только ради того, чтобы конкретный шаблон прошел тест. Если шаблон не совпадает с поведением надстройки, подстраивать шаблон, его входные листы, порядок активации листов и подготовку данных под уже работающую надстройку. Менять надстройку можно только по отдельной прямой задаче пользователя именно на надстройку.
- Для каждого шаблона РД отдельно согласовывать настройки на листе `Панель управления` в блоке `Настройки шаблона`: название, значения, дефолт, этап влияния и поведение при пустом значении. Не переносить настройки из `Е-130_Выручка` в другие РД автоматически; если непонятно, нужны ли `Отсекать ВГО`, `хвост ниже УОНИ`, `делить на 1000` или другие параметры, сначала спросить пользователя.
- Надстройка является отдельной функциональной линией, а не профилем. Для нее не применять правила `profiles/<profile>/` по умолчанию и не создавать `profiles/addin/`.
- Если задача касается переключения надстройки между `Dev` и `Company/Work`, использовать раздел `Как переключать режимы` из `_publish/audit-macros-private/docs/addin-agent-instructions.md` как основной runbook.
- Если меняются кнопки, меню или пользовательские сценарии вкладки `Аудиторам`, вместе с кодом обязательно обновлять встроенную инструкцию внутри надстройки.
- Если в надстройке появляется новая кнопка или новое крупное меню, для них обязательно находить или генерить подходящую по смыслу и стилю картинку и встраивать ее в Ribbon-пакет.
- Для надстройки поддерживать одну основную dev-версию и релизить рабочую версию только из нее по уже зафиксированным правилам.
- Для надстройки считать, что `Dev` и `Company` отличаются только упаковкой и доставкой: `Dev` - это безопасный runtime без VBA-пароля, без `Loader` и без `Setup`; `Company` - это тот же безопасный runtime под VBA-паролем плюс `Loader` и `Setup`.
- Для этой рабочей машины считать эталонной dev-картой путей: `%APPDATA%\AuditAddin\installed\AuditAddin.xlam` - локально установленный runtime; `%APPDATA%\AuditAddin\addin-updater.ini` - локальный конфиг; `%APPDATA%\AuditAddin\rd-constructor\templates` - локальный кэш и fallback шаблонов РД; текущий literal dev-source для сценариев открытия шаблонов РД - `C:\Users\pavel\OneDrive\Рабочий стол\Codex_venv\Макросы для аудита\_publish\audit-macros-private\addin\release\company-archive\2026.04.22.183240\`; в нем должны лежать `AuditAddin.xlam`, `AuditAddin.version.txt`, `templates-rsbu\` и `templates-msfo\`. Соответственно, в dev-конфиге этой машины должны сохраняться literal значения `NetworkPackagePath=C:\Users\pavel\OneDrive\Рабочий стол\Codex_venv\Макросы для аудита\_publish\audit-macros-private\addin\release\company-archive\2026.04.22.183240\AuditAddin.xlam` и `NetworkVersionPath=C:\Users\pavel\OneDrive\Рабочий стол\Codex_venv\Макросы для аудита\_publish\audit-macros-private\addin\release\company-archive\2026.04.22.183240\AuditAddin.version.txt`; dev-установка не должна их стирать при `NetworkEnabled=No`.
- Для company/work на рабочих компьютерах считать эталонной общей структурой `Z:\Project\DAVK\Оборудование\Addins\`: Excel лежит в `Excel\channels\dev|testers|stable`, `Excel\assets` (включая `Excel\assets\Выборка`), `Excel\archive`; Word — в `Word\channels\dev|testers|stable`, `Word\assets`, `Word\archive`; общие служебные данные — в `_system\Office\Access\roles.tsv` и `_system\Office\ActivityLogs\control|data|viewer`. Для параллельного тестового контура тот же контракт применяется к `Z:\Project\DAVK\Оборудование\Addins_Test\`. Каждый канал содержит Setup в корне и `Не трогать` с runtime, version и Loader. Старые верхнеуровневые `addin-*`, `word-addin-*`, `assets`, `word-assets` и общий `archive` не являются рабочей структурой и допустимы только как точные цели `tools/Reset-AuditOfficeNetworkLayout.ps1` при явно запрошенном чистом развёртывании; на этой машине `Z:` по-прежнему не искать, не монтировать и не менять.
- Для Excel company/work локально считать эталоном `%APPDATA%\Microsoft\Excel\XLSTART\AuditAddinLoaderLocal.xlam`, `%APPDATA%\AuditAddin\installed\AuditAddinLocal.xlam`, `%APPDATA%\AuditAddin\assets` и `%APPDATA%\AuditAddin\addin-updater.ini`. Для testers конфиг использует `NetworkPackagePath=Z:\Project\DAVK\Оборудование\Addins\Excel\channels\testers\Не трогать\AuditAddin.xlam`, `NetworkVersionPath=Z:\Project\DAVK\Оборудование\Addins\Excel\channels\testers\Не трогать\AuditAddin.version.txt`, `NetworkAssetsRoot=Z:\Project\DAVK\Оборудование\Addins\Excel\assets`, `NetworkSamplingRoot=Z:\Project\DAVK\Оборудование\Addins\Excel\assets\Выборка`, `NetworkRolesPath=Z:\Project\DAVK\Оборудование\Addins\_system\Office\Access\roles.tsv`, `NetworkActivityLogRoot=Z:\Project\DAVK\Оборудование\Addins\_system\Office\ActivityLogs`, `AssetsRoot=%APPDATA%\AuditAddin\assets`, `RdTemplateRoot=%APPDATA%\AuditAddin\assets`; для stable/dev меняется только сегмент канала, а для тестового контура — корень `Addins` на `Addins_Test`. `NetworkAssetsRoot` копируется без `Выборка`, а `NetworkSamplingRoot` — в локальный `AssetsRoot\Выборка` с прежними фильтрами PDF и файлов с пометкой `копия`.
- Для Word company/work локально считать эталоном `Application.StartupPath\AuditWordAddinLoaderLocal.dotm`, dual-slot runtime `%APPDATA%\AuditWordAddin\installed\slots\A|B\AuditWordAddinLocal.dotm`, `%APPDATA%\AuditWordAddin\assets` и `%APPDATA%\AuditWordAddin\word-addin-updater.ini`. Сетевые runtime/version берутся из `Z:\Project\DAVK\Оборудование\Addins\Word\channels\<channel>\Не трогать`, assets — из `Z:\Project\DAVK\Оборудование\Addins\Word\assets`, роли и ActivityLogs — из тех же общих `_system\Office` путей, что у Excel.
- У каждого пользователя одна общая роль из `roles.tsv`, независимая от канала. Отсутствующий, отключённый, некорректный или неизвестный профиль даёт Assistant; Expert даёт полный текущий профиль. Все company-пакеты одинаковы и содержат полный защищённый runtime; Ribbon только скрывает закрытые функции, а публичные entry points обязаны повторно проверять FeatureKey до мутации. Изменение существующей роли требует обновить `roles.tsv`, дождаться Loader sync и полностью перезапустить Office, но не переустанавливать надстройку. Обычные company-release не заменяют `roles.tsv`, `ActivityLogs\control` или `ActivityLogs\data`.
- Если меняется dev-source, company-root, `NetworkSamplingRoot`, роли или раскладка ActivityLogs, агент обязан в той же задаче обновить literal-пути в канонических инструкциях, корневом AGENTS и соответствующих локальном/рабочем конфигах, не оставляя старые пути на потом.
- Для надстройки считать жестким правилом отказоустойчивости: отсутствие, пустота или недоступность любого пути не должно подвешивать Excel. Пользовательские on-demand сценарии должны быстро прерываться с понятным сообщением в окне, а фоновые автосервисы - тихо завершаться с записью в лог/`Debug.Print`, без зависания старта Excel.
- Для надстройки считать, что все ограничения безопасности `Company` действуют и для `Dev`: не использовать интернет, Git как источник обновления, `WScript.Shell`, `Shell(...)`, `powershell.exe`, `cmd.exe`, `curl`, `ExecutionPolicy Bypass`, HTTP-клиенты, Outlook send, а также VBA-механизмы удаления и перемещения файлов и папок вроде `Kill`, `RmDir`, `DeleteFile`, `DeleteFolder`, `MoveFile`, `MoveFolder`, `Name old As new`.
- Для надстройки разрешать только безопасную локальную файловую работу, нужную для сценария add-in: `FileExists`, `FolderExists`, `GetParentFolderName`, `GetFileName`, `CreateFolder`, `CopyFile` в контролируемых сценариях, `ADODB.Stream`, `OpenTextFile`, `Workbooks.Open`, `Workbook.SaveAs`, запись локального конфига, логов и служебных файлов конструктора РД. `Shell.Application` разрешен только как диалог выбора папки через `BrowseForFolder`.
- Для этой рабочей машины после любых изменений в надстройке, которые пользователь потенциально будет проверять руками, нужно локально поставить пользователю самую свежую dev-версию через `addin/build/Install-AuditAddin.ps1` из `addin/release/`, а затем уже закоммитить и запушить актуальное состояние в `main` репозитория `_publish/audit-macros-private`.
- Для этой рабочей машины считать, что пользователь заранее разрешил закрывать любой `EXCEL.EXE`, если это нужно для сборки, smoke-тестов, установки, переключения режима или повторной загрузки dev-надстройки. Дополнительное согласование на закрытие Excel не требуется.
- Для надстройки считать релизные каналы раздельными: `addin/release/AuditAddin.xlam` - это основной dev runtime без VBA-защиты, а `addin/release/company/` - это company-папка, где лежат две итоговые подпапки: `На рабочий комп` для копирования обновления и `Результаты проверок` для визуального контроля релиза.
- Company-пакеты не пересобирать автоматически при каждом изменении dev-версии: собирать `addin/release/company/` только когда пользователь прямо попросил company-release.
- По умолчанию после обычных изменений в dev-версии запускать `addin/tests/Invoke-AddinTests.ps1 -ChangedFiles` или точечный `-Feature`, затем собирать, обновлять `addin/release/`, вручную ставить на этот компьютер самую свежую dev-надстройку, чтобы пользователь мог тестировать ее руками, коммитить и пушить только dev-пакет из `addin/release/`. `publish-company-addin-release.ps1` и обновление `addin/release/company/` не запускать без прямой команды пользователя.
- Для надстройки считать правилом, что любые завершенные изменения нужно фиксировать в Git: после работы коммитить и пушить свои изменения в репозиторий `_publish/audit-macros-private`, если пользователь явно не попросил не пушить. В коммит не включать чужие или нерелевантные локальные хвосты.
- Для add-in runtime считать обязательной проверку чистоты пакета: и в dev-пакете `addin/release/AuditAddin.xlam`, и в company-пакетах `addin/release/company/*.xlam` не должно быть dev-хвостов и запрещенных shell/Git/network/delete/move-маркеров вроде `modAddinUpdaterDev`, `git`, `GitFirst`, `GitEnabled`, `GitRepoUrl`, `GitBranch`, `powershell.exe`, `cmd.exe`, `wscript.exe`, `WScript.Shell`, `Shell(`, `ExecutionPolicy Bypass`, `git clone/fetch/checkout/pull`, `MSXML2.XMLHTTP`, `MSXML2.ServerXMLHTTP`, `WinHttp.WinHttpRequest.5.1`, `Kill`, `RmDir`, `DeleteFile`, `DeleteFolder`, `MoveFile`, `MoveFolder`.
- Для обычной разработки надстройки основной вход тестирования - `addin/tests/Invoke-AddinTests.ps1`: запускать `-ChangedFiles` или конкретный `-Feature`, а не старые огромные smoke-тесты. `source-policy` - единая source-level проверка запрещенных маркеров для dev/company runtime; отдельный company source-scan не нужен, потому что ограничения одинаковые.
- `addin/tests/smoke-test.ps1`, `addin/tests/smoke-built-addin.ps1` и `addin/build/install-smoke-test.ps1` не считать обязательным циклом по умолчанию; запускать их только по явному full/integration/release основанию, когда менялись сборка `.xlam`, импорт VBA, Ribbon, setup/loader или баг воспроизводится только внутри Excel.
- Для company-release считать пайплайн фиксированным: сначала собрать staging-пакет из dev-кодовой базы, затем поставить VBA-пароль, затем один раз прогнать `company-package-policy-smoke-test.ps1` по уже защищенному staging-пакету, потом один раз прогнать `vba-protection-smoke-test.ps1`, и только после этого обновлять `addin/release/company/На рабочий комп/` и `addin/release/company/Результаты проверок/`. Company package policy проверяет готовую упаковку `.xlam`, а не дублирует `source-policy`.
- Для company-release по умолчанию складывать в `addin/release/company/На рабочий комп/` в корень только `AuditAddinSetup.xlam`, а `AuditAddin.xlam`, `AuditAddin.version.txt` и `AuditAddinLoader.xlam` класть в подпапку `Не трогать`. `cbr_currency_2020_2026.xlsx` добавлять туда только по прямой просьбе пользователя или с явным флагом `-IncludeCurrencyWorkbook`. Папки шаблонов РД и `rd-document-map.tsv` не смешивать с обычным релизом надстройки; для них использовать отдельный template-release через `addin/build/publish-rd-template-company-release.ps1`.
- Для этой рабочей машины после каждого company-release дополнительно класть актуальную копию папки `addin/release/company/На рабочий комп/` в `C:\Users\pavel\OneDrive\Рабочий стол\Релиз надстройки Аудиторам`, копировать туда подпапку `Результаты проверок` и создавать рядом архив `На рабочий комп.zip` со всем, что нужно закинуть на рабочий компьютер; эта папка на рабочем столе должна быть зеркалом текущего комплекта для копирования и визуального контроля, а не архивом релизов. По умолчанию в корне там лежат `AuditAddinSetup.xlam`, `На рабочий комп.zip`, `Результаты проверок` и подпапка `Не трогать` с `AuditAddin.xlam`, `AuditAddin.version.txt`, `AuditAddinLoader.xlam`; `cbr_currency_2020_2026.xlsx` попадать туда должен только при явном `-IncludeCurrencyWorkbook`, шаблоны и `rd-document-map.tsv` туда не попадать при обычном релизе надстройки.
- Для company-release сохранять в `addin/release/company/Результаты проверок/` человекочитаемые результаты финальной policy-проверки и финальной проверки VBA-защиты с видимым временем прогона, чтобы пользователь мог открыть их и быстро убедиться, что в релиз не попало ничего лишнего.
- Для company-release, если в сценарии появляется новый путь к файлу или папке и пользователь его явно не указал, агент обязан сначала спросить этот путь у пользователя. Нельзя придумывать новый сетевой или локальный путь по аналогии и нельзя молча менять уже согласованный путь.
- Если пользователь просит company-release конкретного шаблона РД, это означает один чистый защищенный `.xlsm` этого шаблона: без вставленных выгрузок, без прогнанных макросов, без листов `Модель_*`, `Входные данные_*`, `Отбор_*`, `Результат_*`. Три артефакта `_01/_02/_03` являются только тестовым комплектом и не являются company-release. Такой одиночный release класть в `C:\Users\pavel\OneDrive\Рабочий стол\Артефакты шаблонов РД\<имя шаблона>\company_release\` с отчетами в подпапке `Результаты проверок`. Глобальный комплект `_publish/audit-macros-private/addin/release/rd-templates-company/` с `templates/`, `templates-rsbu/`, `templates-msfo/` и `sync/rd-document-map.tsv` собирать только если пользователь явно попросил общий template-release всех шаблонов. Dev-шаблоны остаются без VBA-пароля; VBA-пароль ставится только на macro-enabled company-копии. Каждый macro-enabled шаблон должен иметь собственный `modRdTemplateLogger` и не должен вызывать логгер запароленной надстройки.
- `addin_updater/tests/smoke-test.ps1` не считать частью обязательного цикла разработки надстройки. Гонять его только когда задача прямо затрагивает `addin_updater/` или company/work-механику доставки.
- Сборки и тесты вида `no-updater` и `diagnostic` считать obsolete-хвостами: не использовать, не восстанавливать и не развивать без прямого запроса пользователя.
- Локальный путь для company-релизов: `_publish/audit-macros-private/addin/release/company/`. При каждом новом company-release старые файлы переносить в `_publish/audit-macros-private/addin/release/company-archive/`, создавая подпапку с версией (например, `2026.04.19.092456/`). В самой папке `company/` хранить только актуальную сборку.

## Общие правила
- В начале каждой задачи, затрагивающей Git-репозиторий, фиксировать исходный `git status --short -uall` и отдельно учитывать уже существующие пользовательские изменения. Каждый созданный в задаче путь к завершению либо коммитить, либо удалять; нельзя оставлять новые untracked/ignored хвосты. Чужие незавершенные изменения не стадить и не удалять: при необходимости изолировать работу. Перед финальной передачей запускать корневой `tools/Assert-CleanWorktree.ps1`: он проверяет и этот workspace, и вложенный `_publish/audit-macros-private`. Завершенная задача должна оставить оба репозитория чистыми, если пользователь прямо не потребовал иного.
- Каждый новый диалог считать вне контекста прошлых обсуждений, если пользователь явно не сказал обратное.
- Не собирать контекст по проекту автоматически: не сканировать репозиторий, не читать историю Git и не изучать файлы без прямой необходимости.
- Работать только от текущего запроса пользователя.
- Если данных не хватает, задать один короткий уточняющий вопрос.
- Не придумывать новые архитектурные правила сверх зафиксированных ниже без согласования с пользователем.

## Репозиторий макросов
- Все `*.bas` хранить в репозитории `https://github.com/pashkarp14/audit-macros-private`.
- Ветку по умолчанию использовать `main`, если пользователь явно не указал другую.

## Google-хранилище макросов для ручного копирования
- Для сохранения, обновления или поиска макросов в пользовательском Google-хранилище сначала читать `_publish/audit-macros-private/docs/google-macro-storage-agent-instructions.md`. Там зафиксированы точная таблица, раскладка каталога, доступ через Сафонова на `claw` и порядок публикации.
- Google Sheets служит каталогом, а Google Docs — местом хранения полного VBA-кода для ручного копирования на рабочем компьютере. По запросу нового самостоятельного макроса сразу готовить отдельный Google Doc; не спрашивать о размещении в общих модулях или профилях и не подключать legacy-updater. Проверенную текстовую копию можно сохранять в `docs/google-macro-bundles/` канонического репозитория.
- Завершая разработку или исправление макроса именно для этого ручного хранилища, синхронизировать проверенную версию с Google Docs и записью каталога, если пользователь не ограничил задачу локальными изменениями. Не выгружать автоматически весь репозиторий, надстройку или все шаблоны РД.
- Существующий модуль обновлять по прежнему document ID, сохраняя ссылку. Для нового модуля создавать отдельный документ; перед созданием проверять каталог на дубли. Разные шаблоны и варианты выборки с одинаковыми названиями процедур не считать одним модулем.
- Публиковать полный код модуля для вставки в VBA без экспортных `Attribute ...`, Markdown-ограждений и пояснений среди кода. Место вставки, зависимости, источник, версию и результат проверки указывать в примечании к ссылке каталога или в отдельной инструкции.
- После записи повторно прочитать документ и измененные ячейки, сверить код и ссылки. Если доступа нет или проверка не прошла, прямо сообщить о незавершенной синхронизации; не выдавать локальный файл за обновление Google.
- При просьбе посмотреть только названия читать каталог и метаданные документов, не загружая тела всех макросов. Справочный снимок названий: `_publish/audit-macros-private/docs/google-macro-catalog.md`; перед публикацией сверяться с живой таблицей.

## Правила кодировки VBA
- Исходные `.bas` можно хранить в репозитории как UTF-8, но VBE/Excel нельзя кормить такими файлами напрямую, если внутри есть русский или другой non-ASCII текст.
- Перед любым импортом `.bas` в VBE из PowerShell, smoke-теста, сборщика или разовой COM-проверки сначала создать временную ANSI-копию: прочитать исходник как UTF-8, записать временный файл через `Set-Content -Encoding Default`, импортировать временный файл, затем удалить временную папку.
- Не использовать прямой `$vbProject.VBComponents.Import($utf8BasPath)` для исходных `.bas` с русскими строками: MsgBox и тексты в ячейках превратятся в кракозябры.
- Если в Excel появилось окно с кракозябрами, сначала считать причиной неправильный импорт/кодировку теста или сборки, а не текст самого VBA-модуля.
